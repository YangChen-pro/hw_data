# 1.2.1 HTTP协议Portal认证流程

此过程中如下几点需要关注：

- WLAN无线场景在AP上完成，而不是AC。

- Portal服务器和RADIUS服务器虽然在大部分场景下是同一个服务器，但是从逻辑和功能上来说，是两个网元。

- 如果探测报文触发重定向时提示证书不可信，请替换AC或者Portal服务器的证书解决，具体内容请参见 跳转到设备登录URL，提示安全告警 。

- 如果手动或者自动访问HTTPS网站触发重定向时提示证书和域名不匹配，该问题请参见 访问HTTPS网页无法重定向（HSTS协议） 。
